import React from 'react';
import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>You can do everything you want</p>
    </footer>
  );
};

export default Footer;
